processing_level: '{{ processing_level }}'
queue: '{{ queue }}'

# input dataset's name(s)
input_datasets:
{% for in_ds in input_ds %}
  {{ in_ds['name'] }}:
    origin_url: '{{ in_ds['origin_url'] }}'
    path_in_babs: '{{ in_ds['path_in_babs'] }}'
    unzipped_path_containing_subject_dirs: '{{ in_ds['unzipped_path_containing_subject_dirs'] }}'
    is_zipped: {{ 'false' if not in_ds['is_zipped'] else 'true' }}
{% endfor %}

# container ds:
# The image path is not recorded here: it is read from the container dataset's
# datalad-containers registration when needed, so it cannot go stale.
# An explicit `container_images:` list is still honored if one is added.
container:
  name: '{{ container_name }}'
  path_in: '{{ container_ds }}'
